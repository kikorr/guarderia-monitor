Aquí van tus datos privados, un fichero por dato, con SOLO el valor (sin espacios ni salto de línea).
Nombres: `wl_user`, `wl_pass`, `tg_bot_token` y, solo para el fichaje, `fichaje_url` y `fichaje_dni`.
Esta carpeta no se sube a GitHub (está en `.gitignore`); no compartas su contenido con nadie.
